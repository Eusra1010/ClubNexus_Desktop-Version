<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>

<StackPane xmlns="http://javafx.com/javafx"
           xmlns:fx="http://javafx.com/fxml"
           fx:controller="org.example.StudentLoginController"
           styleClass="root">

    <VBox alignment="CENTER"
          spacing="20"
          maxWidth="360"
          fillWidth="true"
          styleClass="login-box">

        <Label text="STUDENT LOGIN"
               styleClass="login-title"/>

        <VBox spacing="6">
            <Label text="Student Roll/ID"
                   styleClass="white-label"/>

            <TextField fx:id="roll"
                       promptText="Enter roll"
                       styleClass="underline-field"/>
        </VBox>

        <VBox spacing="6">
            <Label text="Password"
                   styleClass="white-label"/>

            <HBox spacing="6" alignment="CENTER_RIGHT">

                <PasswordField fx:id="passwordField"
                               promptText="Password"
                               styleClass="underline-field"
                               HBox.hgrow="ALWAYS"/>

                <TextField fx:id="visiblePasswordField"
                           promptText="Password"
                           styleClass="underline-field"
                           visible="false"
                           managed="false"
                           HBox.hgrow="ALWAYS"/>

                <Button text="👁"
                        onAction="#togglePassword"
                        styleClass="eye-btn"/>
            </HBox>
        </VBox>

        <Button text="LOGIN"
                onAction="#onLogin"
                maxWidth="Infinity"
                prefHeight="50"
                styleClass="login-btn-white"/>

    </VBox>
</StackPane>
